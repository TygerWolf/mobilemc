<div id="Track">

  <%=render :partial => 'tracks' %>

</div>

<%= link_to "edit", edit_track_path(@track.id) %>

<%= link_to "delete", @track, :method => :delete ,:confirm => "Are you sure?" %>

<script>
  $('p').first().prepend('<h2>HOOK</h2>');
  $("p:gt(0)").prepend('<h2>VERSE</h2>');
  $("p:gt(0)").append('<button type="button">HOOK</button>');
</script>